export const config = {
  github: {
    login: 'glink25', // github login name, not user name
    repo: "test-for-anything", //"urodele",
    logInUrl: '',
    logInAuthUrl: '',
  },
  head: {
    title: 'Urodele'
  }
} as const;

export default config;
